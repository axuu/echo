# Echo

`echo` starts the local Echo browser service from npx.

```bash
npx echo
npx echo start --port 3839
npx echo doctor
```

Python 3.12 is required. On first run, Echo creates a managed Python virtual environment under the user's local app data directory.
